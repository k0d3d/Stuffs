#modal-1.md-modal.md-effect-1
  .md-content
      h3 {{modal.heading}}
      div
        p {{modal.body}}
        .well(ng-show="pt")
          ul.prescriptiontinz
            li(ng-repeat="p in dispenseform.prescription")
              
        button.md-close Close
        button Dashboard